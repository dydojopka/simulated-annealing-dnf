\documentclass[14pt]{extarticle} %Класс позволяет использовать базовые шрифты бОльших размеров
\usepackage{imit_vkr}

%=============================
%Персональная настройка макета
%Здесь могут располагаться дополнительные команды для персональной тонкой настройки
%=============================

%=============================
%Конец Персональная настройка макета
%=============================

%Подключение литературы
\addbibresource{literature.bib}
\begin{document}
	
%%%--------Титульная страница
\input{cover.tex}
%%%----------------------Содержание дипломной работы%%%
\renewcommand{\baselinestretch}{1.5}
\normalsize
%-------------
%Содержание
%-------------
\renewcommand{\contentsname}{СОДЕРЖАНИЕ}
\noindent\tableofcontents

%-------------
%Основная часть
%-------------
\mynonumbersection{ВВЕДЕНИЕ}

биба

\setlength{\itemsep}{0mm}

боба

%=======================

\mysection{Описание предметной области}
\mysubsection{Анализ предметной области}
абоба

Стартовая страница веб-приложения представлена на рис.%~\ref{first}.
% \begin{figure}[h]
% \centering
% %Рамка только для изображений с белым фоном
% \fbox{\includegraphics[scale=0.7]{img/import.png}}
% \caption{Веб-приложение phpMyAdmin}
% \label{first}
% \end{figure}

\mysubsection{Требования к разрабатываемому приложению}

Формирование требований к разрабатываемому веб-приложению началось с анализа типовых сценариев использования и технических
ограничений, с которыми сталкиваются современные команды. В результате
был определён перечень функциональных и технических характеристик, без которых реализация подобного сервиса представляется затруднительной.

Функциональные требования:
\begin{itemize}[label=--]
\item регистрация и аутентификация пользователей с использованием JWT —
стандартная практика для современных веб-приложений, где важна безопасность и независимость от состояния сессии;
\item обмен текстовыми сообщениями и файлами в реальном времени;
\item отслеживание онлайн-статуса пользователей;
\item контроль доступа к ресурсам с учётом проверки прав — вопрос, который
нельзя оставлять без внимания.
\end{itemize}

Технические требования:
\begin{itemize}[label=--]
\item архитектура «клиент—сервер» с чётким разделением фронтенд- и бэкенд компонентов;
\item серверная часть строится на Spring Boot с использованием паттерна MVC
и разделением на слои: контроллер, сервис, репозиторий, модель;
\item в качестве хранилища данных используется реляционная СУБД MySQL;
\item файловые вложения сохраняются непосредственно в базе данных;
\item для обмена сообщениями и синхронизации данных в реальном времени
применяется WebSocket (STOMP);
\item документирование REST API осуществляется через Swagger;
\item клиентская часть реализуется на React с использованием современных
библиотек (Redux Toolkit, TailwindCSS, React Hook Form и др.);
\item поддержка асинхронных запросов, а также механизмов работы с формами, валидации и обработки ошибок как на клиенте, так и на сервере;
\end{itemize}

Все помещенные в курсовую иллюстрации (различные схемы, графики, фотографии) именуются рисунками. Размер рисунка не должен превышать принятого для шаблона формата бумаги. Подпись к рисунку размещается непосредственно под ним, выравнивание <<по ширине>>, со стандартным отступом слева. Рисунок помещается сразу после упоминания о нем в тексте. Каждая таблица должна иметь заголовок. Наименование <<Таблица>> с соответствующим номером, помещают над таблицей, используя выравнивание <<по правому краю>>, затем помещают заголовок, используя форматирование <<по центру>>. Сокращения слов в таблице недопустимы. Для всех приведенных в таблице характеристик должны быть указаны единицы измерения и их размерность. Если таблица располагается на нескольких страницах, то каждая последующая страница оформляется определенным образом. Над переносимой частью таблицы, справа пишется <<Продолжение табл.>> или <<Окончание табл.>> и указывается ее номер. При переносе части таблицы на другие страницы название помещают только над первой частью таблицы.

Таблицы и рисунки помещаются в тексте после абзацев, содержащих ссылку на них, а если такой возможности нет, то с первого абзаца на следующей странице. Нумерация таблиц и рисунков сквозная для всей ВКР.

Уравнения и формулы выделяются из текста в отдельную строку. Формула в отдельной строке должна располагаться по центру. Символьные составляющие и числовые коэффициенты формулы расшифровываются. Пояснения значений символов и числовых коэффициентов следует приводить непосредственно под формулой в той же последовательности, в которой они даны в формуле. Первую строку пояснения начинают со слова <<где>> без двоеточия. В конце каждой строки ставят точку с запятой, в конце последней --- точку. В тексте ссылки на формулу даются аналогично ссылкам на таблицу.

Ссылки в тексте делаются следующим образом:
\begin{itemize}[label=--]
	\item на формулу --- формула (2);
\item на рисунок в тексте --- рис. 2;
\item на таблицу --- табл. 3;
\item на приложение --- прил. 1;
\item на стандарты --- (ГОСТ 7.32--2001);
\item на литературу --- [2].
\end{itemize}

\mysubsection{Реализуемые варианты использования}

Далее следует представить варианты использования, указанные в документе <<Видение>>.

Пример таблицы
\begin{table}[h]
\centering
\caption{Таблица FAT}
\label{t1}
\begin{tabular}{r|c|l|}
\cline{2-3}
 8 & 13 & Начало файла D  \\ \cline{2-3}
 7 &  0 &   \\ \cline{2-3}
 6 &  5 &   \\ \cline{2-3}
 5 & -1 &   \\ \cline{2-3}
 4 &  0 & Начало файла B  \\ \cline{2-3}
 3 & -1 &   \\ \cline{2-3}
 2 &  3 &   \\ \cline{2-3}
 1 &  0 &   \\ \cline{2-3}
 0 & 10 &   \\ \cline{2-3}
\end{tabular}
\end{table}

%==============
\mysection{Обзор технологий разработки}
\mysubsection{Spring Boot}

Серверная часть построена на фреймворке Spring Boot []. Данный инструмент, несмотря на широкое распространение, остаётся актуальным для задач, связанных с быстрым развертыванием и поддержкой масштабируемых
веб-приложений на Java.

\mysubsubsection{Первый подпараграф}

Детальное описание технологии разработки  


%==========================
\mysection{Описании реализации приложения}

Иногда в начале главы можно кратко описать ее содержимое. Этот прием применяется том случае, когда глава состоит из нескольких параграфов и имеет значительный объем.
 
\mysubsection{Хранимые сущности и проектирование структуры классов}

В работе~\cite{Kotel04} рассматривается ... В работах~\cite{cher, W3Schools:mediatypes, W3Schools:html} приведены алгоритмы ...

В прил.~\ref{app-diagram} на странице~\pageref{app-diagram} продемонстрирована диаграмма классов архитектуры информационной системы. 

При возникновении ошибок подключения срабатывает механизм исключений \lstinline{PDOException}. Для того, чтобы отловить исключение, поместите операции с PDO в блок \lstinline{try ... catch}. С помощью метода \lstinline{getMessage()} можно получить сообщение об ошибке, вызвавшей исключение. Пример такого подключения приведен в листинге~\ref{php_mysql_connect_ex}.  Чтобы закрыть соединение, нужно уничтожить объект, это можно сделать путем присвоения переменной, которая содержит объект, значения \lstinline{null}. Уничтожение объекта обязательно в том случае, когда после получения данных предполагается достаточно большая их обработка.  

%----!!!Обратите внимание на шапку листинга!!!
\begin{lstlisting}[language=PHP, caption={Устанока соединения с базой данных}, label=php_mysql_connect_ex]
$user = 'root';
$pswd = '';
try {
	$db = new PDO('mysql:host=localhost;dbname=world', $user, $pswd);
	foreach($db->query('SELECT * FROM cities') as $row)
		print_r($row);
	$db = null;
} catch (PDOException $e) {
	print "Error!: " . $e->getMessage() . "<br/>";
	die(); //Процесс нужно останавливать
}
\end{lstlisting}

\mysubsection{Разработка серверной части веб-приложения}

Серверная часть приложения реализована на платформе Spring Boot, причём структура кода с самого начала проектировалась с чётким разделением на
слои: контроллеры, сервисы, репозитории и модель данных.  Масштабируемость и вопросы безопасности при этом решаются не отдельными «заплатками», а закладываются в архитектуру изначально.

\mysubsection{Разработка клиентской части веб-приложения}

Клиентская часть приложения разрабатывалась на основе React 19, что
во многом определило архитектурные и технологические решения.

В основе фронтенда лежит компонентная модель React, позволяющая
строить интерфейс из изолированных, переиспользуемых блоков. Для управления состоянием используется Redux Toolkit — инструмент, который, несмотря на
свою популярность, требует вдумчивого подхода к проектированию хранилища
данных. В данном случае централизованное хранение информации о пользователях, чатах, сообщениях и виджетах оказалось оправданным: это упростило
синхронизацию между различными частями приложения и снизило вероятность
ошибок, связанных с рассогласованием состояния.

Далее расположен пример программного кода на языке программирования платформы <<1С:Предприятие>>
\begin{lstlisting}[caption={Подключение механизма сканирования}, label=scaner]
&НаКлиенте
Процедура Сканировать(Команда)
 #Если МобильноеПриложениеКлиент Тогда
  Если СредстваМультимедиа.ПоддерживаетсяСканированиеШтрихКодов() Тогда
   ОбработчикСканирования = Новый ОписаниеОповещения(
    "ОбработкаОтсканированногоШтрихкода", ЭтаФорма);
   СредстваМультимедиа.ПоказатьСканированиеШтрихКодов(
    "Сканируйте Штрихкод", ОбработчикСканирования,,
     ТипШтрихКода.Линейный);
  Иначе Сообщить("Сканирование штрихкодов не поддерживается!");
  КонецЕсли;
 #КонецЕсли
КонецПроцедуры
\end{lstlisting}

\mysubsection{Реализованная функциональность}

В ходе разработки было реализовано веб-приложение, сочетающее функциональность современного мессенджера и платформы для совместной работы.
Основные сценарии использования представлены ниже; каждый из них иллюстрирует отдельный аспект взаимодействия пользователя с системой и отражает
ключевые возможности реализованного решения.

В приложении реализованы следующие варианты использования.

%-------------
%Заключение 
%-------------
\mynonumbersection{ЗАКЛЮЧЕНИЕ}

В ходе выполнения работы была спроектирована и реализована система,
предназначенная для обмена сообщениями и организации совместной деятельности в малых группах. 
Поставленные задачи решались поэтапно, что позволило обеспечить последовательное развитие функциональности и избежать избыточной сложности
на отдельных этапах реализации

В результате выполнения курсовой работы были получены следующие результаты:

\begin{itemize}
\item результат 1; 
\item результат 2;
\item результат 3;
\item результат 4.  
\end{itemize}


    
%-------------
%Список литературы
%-------------
\newpage
%Для включения в диплом списка литературы подключается файл literature.bib
%В нем приведены наиболее часто встречающиеся типы библиографических ссылок
\renewcommand{\refname}{СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ}
\addcontentsline{toc}{section}{\refname}
\printbibliography

%-------------
%Приложения
%-------------
\addappendix{UML-диаграмма классов основного модуля приложения}
\label{app-diagram}

В приложении можно разместить основные элементы исходного текста программы или большие рисунки.  

\addappendix{Реализация алгоритма аналитики}
\label{app-results}

В приложении можно разместить основные элементы исходного текста программы или большие рисунки.  

\end{document}

